{
  "name": "study-agent-app",
  "version": "1.0.0",
  "private": true,
  "description": "学习助手 Android App（Capacitor 封装网页版）",
  "scripts": {
    "cap:add": "cap add android",
    "cap:sync": "cap sync android",
    "apk:debug": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
