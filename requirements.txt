# 学习助手（飞书机器人 + 网页版）运行依赖
Flask>=3.0
gunicorn>=21.2
requests>=2.31
watchdog>=3.0
pypdf>=4.0
PyMuPDF>=1.23
python-pptx>=0.6.23
python-docx>=1.2.0
APScheduler>=3.10
chromadb>=1.5
PyYAML>=6.0.3

# 免费本地图片/扫描PDF OCR（推荐，轻量且无Paddle兼容问题）
rapidocr-onnxruntime>=1.4

# 免费本地向量模型（知识库检索，无需硅基流动余额）
fastembed>=0.4

# 可选：PaddleOCR 兜底（体积大，按需安装）
# paddleocr>=2.7
# paddlepaddle>=2.6

# study_bot（Telegram 机器人）另需：openai、pyTelegramBotAPI、scipy、python-dotenv
